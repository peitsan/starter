// @starter/core/registry — 命令注册器（CLI / MCP / daemon 共享）
export {
  REGISTRY,
  COMMANDS,
  type CommandContext,
  type CommandDef,
  type CommandResult,
  type CommandRegistry,
} from './commands.js';
