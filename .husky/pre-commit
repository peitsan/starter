#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# pnpm: use dlx to run lint-staged; fallback to npx if not in pnpm
if command -v pnpm >/dev/null 2>&1; then
  pnpm exec lint-staged
else
  npx --no -- lint-staged
fi
